{
  "name": "optix-openapi-client",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint": "tsc && next lint",
    "test": "vitest",
    "test:once": "vitest run",
    "test:debug": "vitest --inspect-brk --no-file-parallelism",
    "test:coverage": "vitest run --coverage --coverage.reporter=text"
  },
  "dependencies": {
    "@types/node": "20.6.0",
    "typescript": "^5.5.3"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "openapi-typescript": "^7.0.2",
    "typescript": "^5.5.3",
    "vitest": "^1.6.0"
  }
}